import java.sql.*;
import java.util.*;

public class Room{

    String roomID;
    String roomName;
    int beds;
    String bedType;
    int maxOccupancy;
    int basePrice;
    String decor;

    public Room(){
        roomID = roomName = bedType = decor = null;
    }

    public String toString(){
        return (roomID + ", " + roomName + ", " + bed + ", " + bedType + ", " + maxOccupancy + ", " + basePrice + ", " + decor);
    }
}
